<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.cooking.star.log.LogMapper">
<insert id="insertLog" parameterType="LogDTO">
INSERT INTO "LOG"
VALUES (DEFAULT,#{username},#{logTitle},CURRENT_DATE)
</insert>


<select id="logRanking" resultType="java.util.HashMap">
       SELECT 
        TRIM(REGEXP_REPLACE("LOG_TITLE", '레시피|추천|만들기|만드는법|황금레시피', '', 'g')) AS "keyword", 
        COUNT(*) AS "count"
    FROM "LOG"
    WHERE "LOG_DATE" = CURRENT_DATE
    GROUP BY "keyword"
    ORDER BY "count" DESC
    LIMIT 5
    </select>
</mapper>